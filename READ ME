I couldn't get the imbalanced module to import to anaconda/jupyter notebook. That being said, I think the code I provided works as if I had the module along with an explanation of what I would do if I knew the outputs sought for this homework assignment.
